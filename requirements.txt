# Multi Toolkit installs these on demand at runtime, so this file is optional.
# Use it to pre-install everything up front:  pip install -r requirements.txt

# Core — always needed
pypdf>=4.0

# PDF rendering, previews and page thumbnails
pypdfium2>=4.0

# Compression, image work and QR rendering
pikepdf>=8.0
Pillow>=10.0

# QR code encoding
qrcode>=7.4

# PDF -> Markdown (layout analysis, figure cropping)
PyMuPDF>=1.24

# Conversions
pdf2docx>=0.5
python-pptx>=0.6

# Video downloads (YouTube / Reels / Shorts / TikTok)
yt-dlp>=2024.1.1
